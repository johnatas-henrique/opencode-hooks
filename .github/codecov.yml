coverage:
  precision: 2
  round: down
  range: "70...100"
  status:
    project:
      default:
        target: auto
        threshold: 5%
        base: auto

        if_ci_failed: error
comment:
  layout: 'reach,diff,flags,files,footer'
