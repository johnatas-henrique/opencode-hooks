name: Auto Assign

on: pull_request

permissions:
  issues: write

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions-ecosystem/action-add-assignees@v1
        with:
          assignees: 'johnatas-henrique'
          github_token: ${{ secrets.GITHUB_TOKEN }}
